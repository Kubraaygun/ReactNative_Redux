import {StyleSheet} from 'react-native';
import Colors from '../theme/colors';

// define your styles
const defaultScreenStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colorsors.WHITE,
    padding: 10,
  },
});
export {defaultScreenStyle};
